# Jira-Ticket-Transition-Status-Webscrape
This is a bash script to extract the statuses that a Jira ticket went transitioned through from each ticket's expanded changelog. The output is the ticket ID, author, the status changed FROM, the status changed TO, and the date the change occured in ISO8601 format.
